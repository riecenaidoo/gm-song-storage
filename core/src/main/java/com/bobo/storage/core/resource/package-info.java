/**
 * The resource layer of the application.
 *
 * <p>This package contains classes that control access to persistent stores.
 *
 * <p>Method definitions should be grouped by CRUD operation. Within each group, singular operations
 * should be defined before batch operations. Method order and parameter lists should reflect the
 * field order of the associated entity (see {@link com.bobo.storage.core.domain}).
 *
 * <p>These classes may be aware of other layers or technical concerns, but must not depend on them.
 */
package com.bobo.storage.core.resource;
